name: My First WorkFlow

on:push

jobs:
    first_job:
        runs_on:ubuntu_latest
        steps:
            - name:Welcome message
            run:echo "My first Github Actions Job"

            - name:List files
            run:ls

            - name:read file
            run:cat README.md
            
